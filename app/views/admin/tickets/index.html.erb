<h1><%= t('.title') %></h1>

<table class="flat-table">
  <thead>
  <tr>
    <th><%= t('.id') %></th>
    <th><%= t('.email') %></th>
    <th colspan="3"><%= t('.name') %></th>
  </tr>
  </thead>

  <tbody>
  <%= render @tickets %>
  </tbody>
</table>

<%= link_to t('common.back'), root_path, class: 'link-bt' %>
